'use strict';

var colors = require('picocolors');
var vite = require('vite');
var config = require('./lib-d0e65f8e.js');
var build = require('./lib-c2c1cedb.js');
require('node:path');
require('node:fs');
require('node:url');
require('node:module');
require('esbuild');
require('node:child_process');
require('magic-string');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var colors__default = /*#__PURE__*/_interopDefaultLegacy(colors);

async function preview(inlineConfig = {}, options) {
    if (!options.skipBuild) {
        await build.build(inlineConfig);
    }
    const logger = vite.createLogger(inlineConfig.logLevel);
    config.startElectron(inlineConfig.root, logger);
    logger.info(colors__default["default"].green(`\nstart electron app...`));
}

exports.preview = preview;
