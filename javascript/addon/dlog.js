var LogIsTrue = 1;
let logTYPE = ["Init: ", "LOG: ", "WARN: ", "error "];
let logFILENAME = ["dlog.js ", "email.js ", "print.js ", "main.js"];
let logFN = ["email.genEmail ", "print.out", "print.outError", "print.outWarn"];
let logMETHOD = ["print", "email"];
let = logSUGUR = 1
export function DLOG(LTYPE, FNAME, FNNAME, METLOG, Data) {
  if (LogIsTrue == 1) {
    console.log([LTYPE]+" " +logFILENAME[FNAME] +" FILE:"+logFN[FNNAME]+" " +logMETHOD[METLOG] +" " +
    );
  }
}
